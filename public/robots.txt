# Public marketing and catalogue pages may be crawled. Robots directives are
# not access controls; private records remain protected by the application.
# An absolute Sitemap declaration can be added after the public domain is
# confirmed. The sitemap itself is available at /sitemap.xml with SITE_ORIGIN.

User-agent: *
# Checkout remains noindex via HTML and X-Robots-Tag. Allow the crawler to
# retrieve that directive; disallow would prevent it from seeing noindex.
Disallow: /api/
